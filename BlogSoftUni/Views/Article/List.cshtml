@model List<BlogSoftUni.Models.Article>

@{
    ViewBag.Title = "List";
}

<div class="container">
    <div style=" color:white; " class="row">
        @foreach (var article in Model)
{
    <div style="margin-top:30px; border: 3px solid white; font-family: Arial; " >
        <article>
            <header>
                <h2>
                    @Html.ActionLink(@article.Title, "Details", "Article", new { @id = article.Id }, null)
                </h2>
            </header>
            <p>
                @article.Content
            </p>
            <div class="pull-left">
                @foreach (var tag in article.Tags)
                {
                    @Html.ActionLink(tag.Name, "List", "Tag", new { @id = tag.Id }, new { @class = "btn btn-default btn-xs" })
                }
            </div>

            <footer>
               
                <small class="author pull-right">
                    --author @article.Author.FullName
                </small>
            </footer>

        </article>
    </div>
}
    </div>
</div>

